import styled from 'styled-components';

export const Container = styled.div`
  width: 4rem;
  height: 4rem;

  /* margin-top: -5rem; */

  /* position: relative; */
  position: absolute;
  top: 0;
`;
